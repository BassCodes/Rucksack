{
  "private": true,
  "devDependencies": {
    "eslint": "^8.45.0",
    "typescript": "^5.1.6",
    "uglify-js": "^3.17.4"
  },
  "scripts": {
    "build": "npx tsc && npm run minify",
    "minify": "npx uglifyjs --compress --mangle --mangle-props --output out/index.min.js -- out/index.js"
  }
}
